//universal multipurpose header in cpp
#include <bits/stdc++.h>

using namespace std;

int main() {
       int a,b;
       int sum = a + b;
       cout << "Enter two numbers: ";

       // Read input from user
       cin >> a >> b;

       // Display the sum
       cout << "Sum: " << sum << endl;

        cout << sqrt(sum)<<endl; //
        return 0;
}
// Above code is a simple C++ program that takes two numbers from the user, adds them, and prints the sum. It also calculates the square root of the sum.